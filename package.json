{
  "name": "blueferret-admin",
  "version": "2.0.0",
  "private": true,
  "description": "Blue Ferret CMS — admin panel and API for the static site",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18"
  }
}
